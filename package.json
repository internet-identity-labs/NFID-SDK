{
  "name": "nfid-sdk",
  "packageManager": "yarn@3.2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@yarnpkg/plugin-workspace-tools": "3.1.1",
    "wsrun": "^5.2.4"
  },
  "scripts": {
    "ws": "yarn run wsrun",
    "test": "yarn ws test"
  }
}
