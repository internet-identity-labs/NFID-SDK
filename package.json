{
  "name": "nfid-sdk",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "wsrun": "^5.2.4"
  },
  "scripts": {
    "ws": "yarn run wsrun",
    "test": "yarn ws test"
  }
}
